# OT Dashboard

이 프로젝트는 React와 Vite로 구축된 웹 대시보드 애플리케이션입니다.

---

## 🚀 시작하기

아래 지침에 따라 개발 및 테스트를 위해 로컬 컴퓨터에서 프로젝트를 설정하고 실행할 수 있습니다.

### 사전 요구 사항

- [Node.js](https://nodejs.org/) (v18.x 이상 권장)
- [npm](https://www.npmjs.com/) (Node.js 설치 시 함께 제공됨)

### 설치 방법

1.  **저장소 복제(Clone)**
    ```sh
    git clone https://github.com/BoB14th-SLiMe/Front.git
    ```

2.  **프로젝트 디렉터리로 이동**
    ```sh
    cd Front
    ```

3.  **의존성 설치**
    ```sh
    npm install
    ```

---

## 📜 사용 가능한 스크립트

프로젝트 디렉터리에서 다음 명령을 실행할 수 있습니다.

### `npm run dev`

개발 모드에서 앱을 실행합니다.
브라우저에서 [http://localhost:5173](http://localhost:5173) (또는 터미널에 표시된 주소)을 열어 확인할 수 있습니다.

코드를 수정하면 페이지가 자동으로 새로고침되며, 콘솔에서 린트(lint) 오류를 확인할 수 있습니다.

### `npm run build`

`dist` 폴더에 프로덕션용 앱을 빌드합니다.
React를 프로덕션 모드로 번들링하고 최상의 성능을 위해 빌드를 최적화합니다.

### `npm run preview`

`dist` 폴더의 프로덕션 빌드를 로컬 서버에서 미리 봅니다. 배포하기 전에 최종 빌드를 확인하는 좋은 방법입니다.

### `npm run lint`

ESLint를 사용하여 프로젝트 파일의 코드 품질 및 스타일 문제를 검사합니다.

---
